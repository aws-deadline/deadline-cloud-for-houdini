black == 25.12.*; python_version >= "3.10"
black == 25.11.*; python_version < "3.10"
coverage[toml] == 7.*
mypy == 1.*
pipgrip == 0.*
pytest == 9.*; python_version >= "3.10"
pytest == 8.*; python_version < "3.10"
pytest-cov == 7.*
pytest-xdist == 3.*
ruff == 0.15.*
twine == 6.*
types-pyyaml == 6.*
# Both are used by test_console_signin_dependencies.py, which parses pyproject.toml.
packaging >= 24.0
tomli == 2.*; python_version < "3.11"
# install_dev_submitter.py resolves dependencies for a Python other than the one running
# it; uv is used there because pip does not evaluate environment markers against
# --python-version. See _build_deps_env.
uv == 0.10.*
